---
title: 一些函数
date: '2025-12-19 11:12:21'
updated: '2025-12-29 10:14:19'
---
# mmap
![](/images/4d786fce88dfc5617a7a7e8d29bac546.jpeg)

```c
mmap (void *start, size_t len, int prot, int flags, int fd, off_t offset);
```

# mprotect
```c

```

# memcpy 类
# memchr
```c
memchr(const void *s, int c, size_t n);
```

1. `s`：待搜索的内存块指针
2. `c`：要寻找的字节（以 int 形式传递，但内部会被转为 unsigned char）
3. `n`：搜索的最大字节数

返回值：找到则返回指向该字节的指针；若前 n 个字节中没找到，则返回 NULL

# puts
![](/images/cb578aa7e2c14ca706385f459cc5976a.png)

puts函数里面一开始就调用到了strlen函数，所以可以通过改libc里面strlen的got表，

把strlen的got表改成one_gadget就可以getshell了

# bzero
将指定的内存区域内容清零

```c
void bzero(void *s, size_t len);
```

# dlopen和dlsym
在 Unix-like 系统（如 Linux）中用于动态加载共享库的函数，它允许程序在运行时加载共享库并获取符号（函数或变量）。该函数通常与 dlsym() 配合使用，后者用于查找共享库中的符号

# setbuf
```c
void setbuf(FILE *stream, char *buffer);
```

可以利用setbuf函数在栈上设置缓冲区，重复输入来实现栈溢出

之前一直没怎么了解过这个函数是怎么用的...

# setvbuf
接着上周的看，这个函数相比setbuf可以设置缓冲区大小

```c
int setvbuf(FILE *stream, char *buf, int mode, size_t size);
```

其中：

1. mode：缓冲模式
+ `_IOFBF`：全缓冲模式，数据会在缓冲区满时或显式调用 `fflush()` 时输出
+ `_IOLBF`：行缓冲模式，在遇到换行符时刷新缓冲区，或者调用 `fflush()` 时刷新
+ `_IONBF`：无缓冲模式，数据会立即输出，不经过缓冲区
1. size：缓冲区大小

# strncpy
接着补充一下，strncpy也会被'\x00'截断，但是还是按照长度n复制过去，只是后面的部分全都变成空字符也就是'\x00'了

# 常见函数截断字符
https://xuanxuanblingbling.github.io/ctf/pwn/2020/12/16/input/

| **函数** | **截断情况** | **截断属性** | **截断字符是否保留** | **截断后加** |
| :--- | :--- | :--- | :--- | :--- |
| read(0,a,0x100) | EOF | 无 | 无 | 无 |
| *a = getchar() | EOF | 无 | 无 | 无 |
| scanf("%c",a) | EOF | 无 | 无 | 无 |
| scanf("%s",a) | EOF 0x09 0x0A 0x0B 0x0C 0x0D 0x20 | 截断字符前有有效内容则截断，如无有效内容则跳过截断字符读后面 | 不保留 | 0x00 |
| sscanf(a,"%s",b) | 0x00 0x09 0x0A 0x0B 0x0C 0x0D 0x20 | 截断字符前有有效内容则截断，如无有效内容则跳过截断字符读后面 | 不保留 | 0x00 |
| gets(a) | EOF 0x0A | 截断字符前无论有无有效内容均截断 | 不保留 | 0x00 |
| fgets(a,256,stdin) | EOF 0x0A | 截断字符前无论有无有效内容均截断 | 保留 | 0x00 |
| sscanf(a,"%[^;];",b) | 0x00 0x3B | 无 | 不保留 | 0x00 |
| sprintf(b,"%s",a) | 0x00 | 无 | 保留 | 无（相当于截断字符不保留，截断后加0x00） |
| strcpy(b,a) | 0x00 | 无 | 保留 | 无（相当于截断字符不保留，截断后加0x00） |
| strcat(b,a) | 0x00 | 无 | 保留 | 无（相当于截断字符不保留，截断后加0x00） |
| strncat(b,a,0x10) | 0x00 | 无 | 保留 | 无（相当于截断字符不保留，截断后加0x00） |
| strncat(b,a,0x10) | 到达拷贝长度 | 无 | 保留 | 如果到达拷贝长度，则自动补上0x00 |


